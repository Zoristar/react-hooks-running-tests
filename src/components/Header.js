import React from "react";

function Header(props) {
      return (
        <h1>
          {/* uncomment the line belowa to pass the test */}
          hello from the Header!
        </h1>
      )
}

export default Header;
